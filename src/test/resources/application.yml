spring:
  profiles.active: test
  datasource:
    url: "jdbc:h2:mem:cms;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE;MODE=MYSQL"
    username:
    password:
    driverClassName: "org.h2.Driver"
    initialization-mode: always
    schema: classpath:db/migration/init_tables.sql
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
